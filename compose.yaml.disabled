services:
  dbservice:
    image: mysql:latest
    container_name: cinemadb
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: cinema
    volumes:
      - db:/var/lib/mysql
volumes:
  db: