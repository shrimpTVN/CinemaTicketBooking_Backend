spring.application.name=ticketbooking

logging.pattern.console=${CONSOLE_LOG_PATTERN:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15})- %cyan(%msg%n)}

spring.jpa.hibernate.ddl-auto=none

#DATABASE-CONNECTION
spring.datasource.url=jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:cinemadb}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:root}
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql = {HIBERNATE_FORMAT_SQL:true}

#---docker---
spring.docker.compose.enabled=false
#spring.docker.compose.file=compose.yml
spring.docker.compose.lifecycle-management=start-and-stop
spring.docker.compose.start.command=up
spring.docker.compose.stop.command=stop

# ─────────────────────────────────────────────────────────────────
# VNPAY SANDBOX CONFIG  (TEST-ONLY – xoá hoặc đổi sang prod khi live)
# ─────────────────────────────────────────────────────────────────
# TmnCode và HashSecret lấy từ https://sandbox.vnpayment.vn/devreg
vnpay.tmn-code=OXT17PHK
vnpay.hash-secret=15ADXC0R5CS9HBBMRC25VAXEQFV5BW71

# URL cổng thanh toán VNPay (sandbox)
vnpay.url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html

# BE return URL: VNPay sẽ redirect user về đây sau thanh toán
# ─────────────────────────────────────────────────────────────────────────────
# Chế độ test LOCAL: trỏ thẳng về FE (VNPay chỉ redirect browser user)
# VNPay sandbox sẽ redirect browser về URL này kèm query params kết quả.
# FE đọc vnp_ResponseCode để biết thành công/thất bại.
#
# URL phải khớp với "Website của đơn vị" đã đăng ký trên tài khoản sandbox.
# → Nếu lỗi "Không tìm thấy website": vào https://sandbox.vnpayment.vn/merchant_webapi
#   → Cập nhật → đổi "Website URL" thành http://localhost:5173
# ─────────────────────────────────────────────────────────────────────────────
# Frontend base URL (chỉ cần sửa IP/port ở đây khi thay đổi)
app.frontend-url=http://10.111.212.204:5173

vnpay.return-url=${app.frontend-url}/booking

# FE URL: BE sẽ redirect user sang FE sau khi xử lý xong
vnpay.fe-success-url=${app.frontend-url}/booking
vnpay.fe-failure-url=${app.frontend-url}/booking